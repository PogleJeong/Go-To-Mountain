import styled from "styled-components";

const Wrapper = styled.div`
    
`

function Navigation() {
    return(
        <Wrapper>
            navi
        </Wrapper>
    )
}

export default Navigation;